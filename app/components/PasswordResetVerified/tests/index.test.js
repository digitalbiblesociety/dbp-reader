// import React from 'react';
// import { shallow } from 'enzyme';

// import PasswordResetVerified from '../index';

describe('<PasswordResetVerified />', () => {
	it('Expect to have unit tests specified', () => {
		expect(true).toEqual(false);
	});
});
