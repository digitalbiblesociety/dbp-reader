language: node_js

node_js:
  - 10

script:
  - npm install
  - npm run test

notifications:
  email:
    on_failure: change

after_success: 'npm run coveralls'

cache:
  yarn: true
  directories:
    - node_modules
